<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Admin Portal</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

    <style>
      :root {
        --primary-bg: #f1f5f9; /* Light Slate Grey */
        --nav-bg: #0f172a; /* Deep Navy/Black */
        --accent-color: #3b82f6; /* Bright Blue */
      }

      body {
        background-color: var(--primary-bg);
        color: #334155;
        font-family: 'Inter', sans-serif;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      /* Navbar Styling */
      .sa-navbar {
        background-color: var(--nav-bg);
        padding-top: 0.75rem;
        padding-bottom: 0.75rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      }

      .sa-navbar .navbar-brand {
        font-weight: 700;
        font-size: 1.1rem;
        letter-spacing: -0.5px;
        color: white;
      }

      .sa-navbar .navbar-brand i {
        color: var(--accent-color);
      }

      /* Nav Links */
      .sa-navbar .nav-link {
        color: #94a3b8 !important;
        font-size: 0.9rem;
        font-weight: 500;
        margin-left: 1rem;
        transition: color 0.2s ease;
      }

      .sa-navbar .nav-link:hover,
      .sa-navbar .nav-link.active {
        color: #fff !important;
      }

      .nav-separator {
        color: #334155;
        margin: 0 10px;
        font-size: 0.8rem;
      }

      /* Logout Button */
      .btn-logout {
        border: 1px solid #334155;
        color: #cbd5e1;
        font-size: 0.85rem;
        border-radius: 6px;
        padding: 6px 16px;
        transition: all 0.2s;
      }

      .btn-logout:hover {
        background-color: #dc2626; /* Red warning color on hover */
        border-color: #dc2626;
        color: white;
      }

      /* Main Content Area */
      .sa-main {
        flex: 1; /* Pushes footer down */
        padding-top: 2rem;
        padding-bottom: 4rem;
      }

      /* Optional: Footer */
      .sa-footer {
        text-align: center;
        padding: 20px;
        font-size: 0.8rem;
        color: #64748b;
        border-top: 1px solid #e2e8f0;
        background-color: white;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark sa-navbar sticky-top">
      <div class="container">
        <a
          class="navbar-brand d-flex align-items-center gap-2"
          href="/super-admin"
        >
          <span>SUPER ADMIN</span>
        </a>

        <button
          class="navbar-toggler border-0 focus-ring focus-ring-dark"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#saNavbar"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="saNavbar">
          <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item">
              <a class="nav-link active" href="/super-admin/products">
                <i class="bi bi-box-seam me-1"></i> Inventory
              </a>
            </li>

            <li class="nav-item d-none d-lg-block">
              <span class="nav-separator">|</span>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="/" target="_blank">
                View Live Store
                <i
                  class="bi bi-box-arrow-up-right ms-1"
                  style="font-size: 0.7em"
                ></i>
              </a>
            </li>

            <li class="nav-item ms-lg-3 mt-2 mt-lg-0">
              <a class="btn btn-logout" href="/logout">Logout</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container sa-main"><%- body %></div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
