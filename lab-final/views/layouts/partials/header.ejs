<nav class="navbar navbar-expand-lg navbar-dark custom-navbar sticky-top">
  <div class="container">
    
    <a class="navbar-brand fw-bold" href="/">
      <i class="fa-solid fa-store me-2"></i>AWESOME SHOP
    </a>

    <button 
      class="navbar-toggler border-0 shadow-none" 
      type="button" 
      data-bs-toggle="collapse" 
      data-bs-target="#navbarSupportedContent" 
      aria-controls="navbarSupportedContent" 
      aria-expanded="false" 
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center gap-3">
        
        <li class="nav-item">
          <a href="/contact-us" class="nav-link">Contact</a>
        </li>

        <li class="nav-item">
          <a href="/cart" class="nav-link position-relative">
            <i class="fa-solid fa-cart-shopping me-1"></i> Cart
          </a>
        </li>

        <% if (locals.isAdmin) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-warning" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-shield-halved"></i> Admin
            </a>
            <ul class="dropdown-menu dropdown-menu-end dropdown-dark shadow-lg">
              <li><a href="/super-admin" class="dropdown-item">Admin Panel</a></li>
            </ul>
          </li>
        <% } %>

        <% if(!locals.user) {%>
          <li class="nav-item d-none d-lg-block text-secondary opacity-25">|</li>
          
          <li class="nav-item">
            <a href="/login" class="nav-link">Login</a>
          </li>
          <li class="nav-item">
            <a href="/register" class="btn btn-primary btn-sm px-4 rounded-pill fw-bold">Register</a>
          </li>
        <% } else { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-user-circle"></i> Account
            </a>
            <ul class="dropdown-menu dropdown-menu-end dropdown-dark shadow-lg">
              <li><a href="/my-account" class="dropdown-item">My Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a href="/logout" class="dropdown-item text-danger">Logout</a></li>
            </ul>
          </li>
        <% } %>

      </ul>
    </div>
  </div>
</nav>

<style>
  /* 1. Navbar Background - Clean Deep Noir */
  .custom-navbar {
    background: #0f0f0f !important; /* Matches our Footer/Detail theme */
    padding: 0.8rem 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  /* 2. Brand Styling */
  .navbar-brand {
    font-size: 1.2rem;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  /* 3. Link Effects */
  .nav-link {
    color: rgba(255, 255, 255, 0.6) !important;
    font-size: 0.85rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: all 0.2s ease;
  }

  .nav-link:hover, .nav-link.active {
    color: #fff !important;
  }

 /* Dropdown Menu - Ensure it sits on top */
.dropdown-dark {
  background-color: #1a1a1a;
  border: 1px solid rgba(255, 255, 255, 0.1);
  margin-top: 10px;
  z-index: 2000;
}

.dropdown-item {
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.85rem;
  padding: 10px 20px;
}

.dropdown-item:hover {
  background-color: rgba(255, 255, 255, 0.05);
  color: #fff;
}



/* ✅ On mobile/collapsed navbar, dropdown must be in-flow */
@media (max-width: 991.98px) {
  .navbar-nav .dropdown-menu {
    position: static;
    float: none;
    width: 100%;
    margin-top: .25rem;
  }
}

</style>